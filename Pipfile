[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
pytest = ">=4.0.0"
pytest-mock = "*"

[packages]
flask = ">=1.0.0"
redis = ">=3.0.0"

[requires]
python_version = "3"
